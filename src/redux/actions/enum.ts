export enum AppActions {
  setAppStatus = "SET_APP_STATUS"
}

export enum DataActions {
  setData = "SET_DATA",
  selectHeader = "SELECT_HEADER",
  selectMenu = "SELECT_MENU",
  selectPrice = "SELECT_PRICE"
}
